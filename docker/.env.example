APP_KEY=
APP_URL=http://localhost:8080
APP_HOST=localhost

POSTGRES_DB=openlink
POSTGRES_USER=openlink
POSTGRES_PASSWORD=openlink

# Change these values before exposing an installation to the internet.
APP_DEBUG=false

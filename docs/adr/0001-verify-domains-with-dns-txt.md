# Verify domains with DNS TXT

Openlink verifies domain ownership with a DNS TXT record before a domain can serve short links. This keeps ownership proof separate from traffic routing, so deployment-specific CNAME or A record choices can evolve without changing the product's verification model.
